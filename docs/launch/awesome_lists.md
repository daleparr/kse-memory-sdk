# awesome-list PR targets (step 3 — only after the blog is live)

One-line description to use everywhere (keep identical for consistency):

> KSE — CPU-only hybrid retrieval with user-defined dimension schemas,
> receipts on every result, and benchmarks that publish their losses
> (`make bench` regenerates the table).

Targets, in order of fit:
- [ ] awesome-information-retrieval
- [ ] awesome-vector-search
- [ ] awesome-rag (framing: the retrieval layer, not a RAG framework)
- [ ] awesome-python (only under a search/IR section; do not stretch)

PR etiquette: one PR per list, follow each list's contribution format
exactly, link the repo and RESULTS.md, no adjectives beyond the one-liner.
