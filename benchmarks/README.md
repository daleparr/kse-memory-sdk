# benchmarks/

Real, reproducible benchmarks only (pinned BEIR subsets + Amazon ESCI slice, CPU-only).
Regenerated by `make bench`. Importing anything from `simulations/` here is a CI failure (AR-02).
Arrives in US5 (Phase 7 of BD8).
